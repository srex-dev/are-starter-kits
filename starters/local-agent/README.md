# Local Agent Starter

Use this for local agents that call tools from a laptop or department runtime.

Pattern:

```text
local agent -> passport scope -> policy check -> tool result or denial
```

This is a local development pattern, not a production RBAC system.
